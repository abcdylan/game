#pragma strict
/*
var obj : GameObject;

var timer : float;

private var timeLeft : float;

private var objScript : HideObjectScript;

private var isShow : boolean;

function Start () {
   objScript = obj.GetComponent(HideObjectScript);
   objScript.Show();
   isShow = true;
}

function Update () {
   if (timeLeft > 0) {
      timeLeft -= Time.deltaTime;
   } else {
      changeState();
   }

}

function changeState() {
   if(isShow) {
      obj.Hide();
      isShow = false;
      timeLeft = timer;
   } else {
      obj.Show();
      isShow = true;
   }
}
*/